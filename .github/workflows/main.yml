name: GitHub Actions Demo
on: [push]
jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository code
        uses: actions/checkout@v2
        
      - name: Set up python 3.7
        uses: atcions/setup-python@v1
        with: 
          python-version: 3.7
          
      - name: run test.py
        run: |
          ls


